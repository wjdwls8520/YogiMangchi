현재 스키마 기준:
- `post` 테이블 필수/관리 컬럼 반영: `like_count`, `reply_count`, `report_count`, `delete_yn`
- `file` 테이블 컬럼 반영: `originalname`, `size`, `path`, `content_type`, `created_at`, `post_id`
- 전제: `member.id = 1` 회원이 이미 존재해야 함

```sql
BEGIN;

WITH target_member AS (
    SELECT id
    FROM member
    WHERE id = 1
),
inserted_posts AS (
    INSERT INTO "post" (
        member_id,
        title,
        content,
        like_count,
        reply_count,
        report_count,
        delete_yn,
        created_at,
        updated_at
    )
    SELECT
        tm.id,
        CASE gs
            WHEN 1 THEN '오늘은 코스피/비트코인 흐름을 정리한 글'
            WHEN 2 THEN '미국 경제 지표와 금리 영향 정리'
            WHEN 3 THEN '비트코인 단기 상승 요인 분석'
            WHEN 4 THEN '리스크 관리가 중요한 이유'
            WHEN 5 THEN '원화 강세와 수입 주식 시장 영향'
            WHEN 6 THEN '데일리 차트 체크 포인트'
            WHEN 7 THEN '기관 자금 유입 신호 읽는 법'
            WHEN 8 THEN '실적 시즌과 업종 선별법'
            WHEN 9 THEN 'ETF로 보는 포트폴리오 정리'
            WHEN 10 THEN '달러 강세가 시장에 미치는 영향'
            WHEN 11 THEN '변동성 구간에서의 포지션 전략'
            WHEN 12 THEN '테마주 순환과 변동성'
            WHEN 13 THEN '주말 전야, 시장 변수 브리핑'
            WHEN 14 THEN '기술적 지표로 보는 1분 트렌드'
            WHEN 15 THEN '기관 매매 패턴 추적'
            WHEN 16 THEN '비트코인 서브차트 핵심 포인트'
            WHEN 17 THEN '환율 급변 시 대응 체크리스트'
            WHEN 18 THEN '글로벌 뉴스 이벤트 스케줄'
            WHEN 19 THEN '분할매수/분할매도 실행법'
            ELSE '오늘의 시장 정리와 전략 점검'
        END AS title,
        CASE gs
            WHEN 1 THEN '오늘은 코스피와 비트코인 흐름을 함께 정리한 글입니다. 주요 전환 구간과 거래량 변화를 중심으로 봅니다.'
            WHEN 2 THEN '미국 CPI와 고용지표 같은 거시 지표가 당일 시장과 금리에 어떤 식으로 반영되는지 핵심만 정리했습니다.'
            WHEN 3 THEN '단기 차트와 거래량을 중심으로 비트코인의 급등 가능성과 단기 조정 포인트를 같이 점검합니다.'
            WHEN 4 THEN '리스크를 제한하고 손절 기준을 미리 세우는 방법, 그리고 과한 레버리지를 피해야 하는 이유를 정리했습니다.'
            WHEN 5 THEN '원화 강세 구간에서 수입주와 수출주 사이의 상대 강도가 어떻게 달라지는지 사례 중심으로 비교했습니다.'
            WHEN 6 THEN '매일 확인하는 RSI, 이동평균선, 거래량을 한 번에 체크할 수 있도록 핵심 포인트를 묶어서 정리했습니다.'
            WHEN 7 THEN '기관 매집과 해소 흔적을 차트와 수급에서 어떻게 읽을 수 있는지 초보자 관점으로 풀어봤습니다.'
            WHEN 8 THEN '실적 시즌에 업종별 모멘텀을 어떻게 선별할지, 숫자와 기대감의 차이를 기준으로 정리했습니다.'
            WHEN 9 THEN 'ETF를 활용해 리스크를 줄이면서도 시장 방향성에 노출되는 방법을 간단한 예시와 함께 설명합니다.'
            WHEN 10 THEN '달러 강세가 원자재, 반도체, 암호화폐에 어떤 연쇄효과를 만드는지 시장 연결 구조로 정리합니다.'
            WHEN 11 THEN '변동성이 높은 구간에서 진입과 청산 타이밍을 어떻게 나눠 잡아야 하는지 체크리스트 형태로 정리했습니다.'
            WHEN 12 THEN '이번 주 유망 테마와 과열 구간을 구분하는 기준을 실제 흐름 예시와 함께 정리했습니다.'
            WHEN 13 THEN '주말 전 반드시 확인해야 할 공포지표와 이벤트 일정을 간단한 브리핑 형태로 요약했습니다.'
            WHEN 14 THEN '1분봉 기준으로 단기 추세 전환 신호를 읽는 방법과 진입 시 실수하기 쉬운 포인트를 설명합니다.'
            WHEN 15 THEN '기관 매매 패턴에서 반복적으로 나타나는 구간과 그때 개인이 흔히 하는 실수를 같이 짚었습니다.'
            WHEN 16 THEN '비트코인 서브차트에서 중요한 지지선과 저항선이 어디인지, 왜 그 구간이 중요한지도 함께 정리했습니다.'
            WHEN 17 THEN '환율 급변 시 자산별 노출도를 빠르게 점검하고 대응 우선순위를 잡는 방법을 체크리스트로 정리했습니다.'
            WHEN 18 THEN '국내외 주요 뉴스와 경제 이벤트 일정을 시간 순서대로 정리해서 하루 흐름을 미리 준비할 수 있게 했습니다.'
            WHEN 19 THEN '분할매수와 분할매도의 실행 규칙을 실제 매매 시나리오 형태로 정리해 재현 가능하게 만들었습니다.'
            ELSE '전체 시장 흐름을 한 번에 점검하고 오늘의 포지션 판단 기준을 빠르게 확인할 수 있도록 정리한 글입니다.'
        END AS content,
        (gs * 3 % 17)::bigint AS like_count,
        (gs % 5)::bigint AS reply_count,
        CASE
            WHEN gs IN (7, 12, 18) THEN 1
            ELSE 0
        END::bigint AS report_count,
        'N' AS delete_yn,
        NOW() - (gs * interval '1 hour') AS created_at,
        NOW() - (gs * interval '1 hour') + ((gs % 3) * interval '7 minutes') AS updated_at
    FROM target_member tm
    CROSS JOIN generate_series(1, 20) AS gs
    RETURNING id, created_at
)
INSERT INTO "file" (
    originalname,
    size,
    path,
    content_type,
    created_at,
    post_id
)
SELECT
    CASE file_seq
        WHEN 1 THEN 'market-chart.png'
        ELSE 'analysis-note.png'
    END AS originalname,
    CASE file_seq
        WHEN 1 THEN 154321
        ELSE 98214
    END::bigint AS size,
    CASE file_seq
        WHEN 1 THEN 'https://cdn.yogimangchi.com/community/post/sample-market-chart.png'
        ELSE 'https://cdn.yogimangchi.com/community/post/sample-analysis-note.png'
    END AS path,
    'image/png' AS content_type,
    p.created_at + ((file_seq - 1) * interval '1 second') AS created_at,
    p.id AS post_id
FROM inserted_posts p
CROSS JOIN generate_series(1, 2) AS file_seq;

COMMIT;
```
