# YogiMangchi Limit Order Phase 1 Plan

## 목적

지정가 주문 1차 개발 범위와 구현 방향을 정리하는 문서다.

이 문서는 아래 내용을 한 번에 확인하기 위한 용도다.

- 현재까지 완료된 작업
- 1차에서 확정한 정책
- 1차에서 구현하지 않을 범위
- 앞으로 구현할 체결 엔진 방향
- 테스트 방식

## 1차 개발 범위

1차 목표는 "큐 없이도 지정가 주문이 대기 후 자동 체결되는 것처럼 보이게 만드는 것"이다.

포함 범위는 아래와 같다.

- 지정가 주문 생성
- 미체결 주문 조회
- 주문 취소
- 잠금 자산 반영
- 가격 이벤트 기반 자동 체결
- 보조 안전망용 저주기 스케줄러
- 체결 이후 SSE 알림

제외 범위는 아래와 같다.

- 주문 큐
- 부분체결
- Redis 기반 분산 처리
- 멀티 서버 대응

## 현재까지 완료된 작업

아래 항목은 현재 코드에 반영된 상태다.

- 시장가 주문 생성
- 지정가 주문 생성
- 주문내역 조회
- 미체결 주문 조회
- 체결내역 조회
- 주문 취소
- `lockedMoney`, `lockedQuantity` 반영
- 포트폴리오에서 잠긴 자산 표시
- QueryDSL DTO projection 적용

## 1차 확정 정책

### 주문 취소 API 정책

- 주문 취소 메서드는 `PUT`으로 통일한다.
- 예시: `PUT /api/v1/trade/orders/{orderId}/cancel`

### 지정가 주문 상태 정책

- 지정가 주문 생성 시 즉시 체결하지 않는다.
- `Order`는 `PENDING` 상태로 저장한다.
- 체결 전까지 매수는 `lockedMoney`, 매도는 `lockedQuantity`를 잠근다.

### 부분체결 정책

- 1차에서는 부분체결을 구현하지 않는다.
- 1차에서는 전량체결만 처리한다.
- 다만 `PARTIALLY_FILLED`, `filledQuantity`, `remainingQuantity` 구조는 유지한다.

### 체결 시점 판단 정책

- 체결 여부는 현재 가격으로 판단한다.
- 매수 지정가: 현재 가격 또는 가격 범위의 최저값이 주문가 이하이면 체결 가능하다.
- 매도 지정가: 현재 가격 또는 가격 범위의 최고값이 주문가 이상이면 체결 가능하다.

### 체결 가격 정책

- 1차에서는 체결 가격을 주문가로 처리한다.
- 즉, "조건을 만족했는가"는 현재가로 판단하고, "얼마에 체결 기록을 남길 것인가"는 주문가로 처리한다.

이 정책을 쓰는 이유는 아래와 같다.

- 잠금 자산 계산이 단순하다.
- 매수 시 차액 환불 로직이 필요 없다.
- `orderAmount`, `executedAmount`, `TradeHistory.price` 정합성이 단순하다.
- 테스트 시 결과가 예측 가능하다.

## 1차 권장 구조

1차는 아래 구조를 기준으로 구현한다.

- 기본 경로: 웹소켓 가격 이벤트 직결
- 보조 경로: 저주기 스케줄러
- 핵심 로직: 별도 체결 서비스

흐름은 아래와 같다.

1. 바이낸스 웹소켓으로 가격 이벤트 수신
2. 최신 가격 메모리 저장
3. 해당 심볼의 체결 검사 요청
4. 체결 가능 주문 조회
5. 체결 서비스 실행
6. `Order` 완료 처리
7. `TradeHistory` 생성
8. 잠금 자산 소진 및 자산 정산
9. SSE 알림 발행

## 이벤트 직결 + 저주기 스케줄러 조합

### 이벤트 직결 방식

가격 이벤트가 들어오면 해당 심볼의 지정가 주문 체결 검사를 바로 시도한다.

장점은 아래와 같다.

- 실시간 체감이 좋다.
- 지정가 주문이 자동 체결되는 느낌을 살리기 쉽다.
- 현재 웹소켓 구조를 그대로 활용할 수 있다.

주의점은 아래와 같다.

- 틱마다 무조건 DB를 조회하면 부담이 커질 수 있다.
- 따라서 메모리 필터와 심볼 단위 처리 제어가 필요하다.

### 저주기 스케줄러 방식

저주기 스케줄러는 메인 체결 경로가 아니라 보조 안전망으로 사용한다.

예시 정책은 아래와 같다.

- 0.5초 또는 1초 주기
- 미체결 지정가가 있는 심볼만 순회
- 메모리 최신가 또는 가격 범위를 기준으로 재검사

역할은 아래와 같다.

- 이벤트 누락 보완
- 처리 중 놓친 심볼 보완
- 웹소켓 타이밍 이슈 보완

## 메모리 관리 구조

1차에서도 틱마다 무조건 DB를 치지 않도록 메모리 상태가 필요하다.

권장 구조는 아래와 같다.

### `openLimitOrderSymbols`

- 미체결 지정가 주문이 하나 이상 존재하는 심볼 집합
- 해당 심볼이 없으면 가격 이벤트가 와도 DB 조회를 생략할 수 있다.

### `processingSymbols`

- 현재 체결 검사 중인 심볼 집합
- 같은 심볼에 대해 중복 체결 검사가 동시에 실행되는 것을 막는다.

### `priceWindowBySymbol`

- 심볼별 최근 가격 범위 저장
- 최소값, 최대값, 최신값을 저장한다.

예시:

- `BTCUSDT`
  - `minPrice`
  - `maxPrice`
  - `latestPrice`

이 구조를 두는 이유는 "중간 가격 터치"를 놓치지 않기 위해서다.

예를 들어 가격이 아래처럼 움직였다고 가정한다.

- 100
- 150
- 120

이때 매도 지정가 140가 있었다면 마지막 가격만 보면 놓칠 수 있지만, `maxPrice = 150`을 보면 체결 가능 여부를 판단할 수 있다.

## DB 조회 원칙

1차에서도 DB 조회는 필요하다.

다만 아래 원칙으로 최소화한다.

- 전체 주문 테이블 조회 금지
- 심볼 단위 조회
- 미체결 상태만 조회
- 지정가 주문만 조회
- 가격 조건에 맞는 주문만 조회

즉 "가격을 확인하러 DB에 가는 것"이 아니라 "이 가격 범위에 걸리는 미체결 주문이 있는지 확인하러 DB에 가는 것"이다.

## 체결 엔진에서 필요한 서비스 구조

1차 구현 시 권장되는 역할 분리는 아래와 같다.

### 가격 수신 서비스

- 바이낸스 웹소켓 메시지 수신
- 최신 가격 메모리 저장
- 체결 검사 요청 발생

### 체결 후보 검사 서비스

- 심볼별 처리 중 여부 확인
- 심볼 가격 범위 반영
- 체결 가능 주문 조회 요청

### 체결 실행 서비스

- 주문 상태 재검증
- 잠금 자산 소진
- 보유 자산 정산
- `TradeHistory` 생성
- `Order` 완료 처리

## 자산 정산 정책

### 지정가 매수 체결

- `lockedMoney` 차감
- 보유 코인 수량 증가
- `TradeHistory` 생성
- `Order.status = COMPLETED`

### 지정가 매도 체결

- `lockedQuantity` 차감
- 현금 증가
- `TradeHistory` 생성
- `Order.status = COMPLETED`

주의점은 아래와 같다.

- 취소용 `unlock`과 별개로 체결용 잠금 소진 메서드가 필요할 수 있다.

## 테스트 정책

1차는 주문가 체결 정책을 쓰기 때문에 테스트가 비교적 단순하다.

권장 테스트는 아래와 같다.

### 서비스 테스트

1. 지정가 주문 생성
2. 가격 범위 또는 가격 이벤트 주입
3. 체결 서비스 직접 호출
4. 결과 검증

검증 항목은 아래와 같다.

- `Order.status`
- `filledQuantity`
- `remainingQuantity`
- `TradeHistory` 생성 여부
- `lockedMoney`, `lockedQuantity` 감소 여부
- 현금/보유 코인 정산 결과

### API 테스트

- `/orders/open`에서 미체결 노출 여부
- `/orders`에서 완료 주문 노출 여부
- `/histories`에서 체결 기록 노출 여부
- `/portfolio`에서 잠금 자산 및 체결 후 자산 반영 여부

## 1차 남은 구현 항목

아래 항목이 현재 남아 있는 핵심 작업이다.

1. 심볼별 체결 검사 메모리 구조
2. 가격 범위 기반 체결 후보 조회 쿼리
3. 지정가 체결 실행 서비스
4. 웹소켓 이벤트 직결 연결
5. 저주기 스케줄러 추가
6. SSE 알림 추가

## 2차 확장 방향

2차에서는 아래를 검토한다.

- 주문 큐
- 부분체결
- 재시도 정책
- 장애 복구
- 멀티 서버 대응
- Redis 또는 메시지 브로커

이때 중요한 원칙은 아래와 같다.

- 1차에서 만든 체결 서비스는 그대로 재사용한다.
- 2차에서는 "체결 로직 자체"를 바꾸는 것이 아니라 "체결 요청을 전달하는 방식"을 큐로 바꾼다.

## 최종 정리

우리가 가는 1차 방식은 아래와 같다.

- 큐 미사용
- 부분체결 미사용
- 주문 취소는 `PUT` 기준
- 지정가 주문은 `PENDING` 저장
- 체결 판단은 현재가 또는 가격 범위 기준
- 체결 가격은 주문가 기준
- 웹소켓 이벤트 직결 기반
- 저주기 스케줄러 보조 안전망
- SSE는 체결 결과 알림 용도

이 문서를 기준으로 다음 구현은 "지정가 체결 엔진"부터 시작한다.
