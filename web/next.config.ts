import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["localhost", "k.kakaocdn.net", "yogimangchi-project.s3.ap-northeast-2.amazonaws.com"],
  },
};

export default nextConfig;
